# QuadraticEquation_Web_System_Solution
Test for Web System Solution

To launch the database, run the docker-compose.yml file with the command: docker-compose up -d.

Technology:

Spring
Hibernate
Thymeleaf
Maven
Lombok
PostgreSQL db
